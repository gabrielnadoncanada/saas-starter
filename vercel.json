{
  "framework": "nextjs",
  "buildCommand": "pnpm exec prisma migrate deploy && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    {
      "path": "/api/demo/reset",
      "schedule": "0 4 * * *"
    }
  ]
}
