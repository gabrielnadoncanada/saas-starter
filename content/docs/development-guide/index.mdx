---
title: Development Guide
description: Daily workflow, feature development, and best practices.
---

Start with these guides:

- `adding-features` for the default boring CRUD path
- `forms` for the standard `useActionState` form pattern
- `server-actions` for mutation boundaries and revalidation
- `action-middleware` for auth and owner/admin guards
- `workflow` for day-to-day editing flow

If you are touching the repo for the first time, read `project-structure` first and then `adding-features`.
